# Local development databases
# Modify as needed for your project

services:
  postgres:
    image: postgres:15-alpine
    container_name: ${PROJECT_NAME:-myapp}-postgres
    environment:
      POSTGRES_USER: ${DB_USER:-app}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-devpassword}
      POSTGRES_DB: ${DB_NAME:-app}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-app}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: ${PROJECT_NAME:-myapp}-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Uncomment if you need MongoDB instead of/in addition to PostgreSQL
  # mongo:
  #   image: mongo:7
  #   container_name: ${PROJECT_NAME:-myapp}-mongo
  #   environment:
  #     MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER:-app}
  #     MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD:-devpassword}
  #   ports:
  #     - "27017:27017"
  #   volumes:
  #     - mongo_data:/data/db

volumes:
  postgres_data:
  redis_data:
  # mongo_data:
